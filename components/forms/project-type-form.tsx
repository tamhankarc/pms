"use client";
import { useActionState } from "react";
import { FormLabel } from "@/components/ui/form-label";
import type { ProjectTypeFormState } from "@/lib/actions/project-type-actions";
const initialState: ProjectTypeFormState = {};
export function ProjectTypeForm({ mode, action, clientId, initialValues }: { mode: 'create'|'edit'; action: (state: ProjectTypeFormState, formData: FormData) => Promise<ProjectTypeFormState>; clientId: string; initialValues?: { id?: string; name?: string; code?: string; isActive?: boolean; }; }) { const [state, formAction, pending] = useActionState(action, initialState); return <form action={formAction} className="card p-6"><input type="hidden" name="clientId" value={clientId}/>{mode==='edit'&&initialValues?.id?<input type="hidden" name="id" value={initialValues.id}/>:null}<h2 className="section-title">{mode==='create'?'Create project type':'Edit project type'}</h2>{state?.error?<div className="mt-4 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{state.error}</div>:null}{state?.success?<div className="mt-4 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">Project type saved successfully.</div>:null}<div className="mt-5 space-y-4"><div><FormLabel htmlFor="name" required>Name</FormLabel><input id="name" name="name" className="input" defaultValue={initialValues?.name ?? ''} required/></div><div><FormLabel htmlFor="code" required>Code</FormLabel><input id="code" name="code" className="input" defaultValue={initialValues?.code ?? ''} required/></div><label className="flex items-center gap-3 rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-700"><input type="checkbox" name="isActive" defaultChecked={initialValues?.isActive ?? true}/>Active project type</label><button className="btn-primary w-full" disabled={pending}>{pending?'Saving...':mode==='create'?'Create project type':'Save changes'}</button></div></form>; }
