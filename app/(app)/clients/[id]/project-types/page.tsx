import Link from "next/link"; import { notFound } from "next/navigation"; import { db } from "@/lib/db"; import { PageHeader } from "@/components/ui/page-header"; import { ProjectTypeForm } from "@/components/forms/project-type-form"; import { createProjectTypeAction, toggleProjectTypeStatusAction } from "@/lib/actions/project-type-actions";
export default async function ClientProjectTypesPage({ params, searchParams }: { params: Promise<{ id: string }>; searchParams?: Promise<{ create?: string }>; }) { const { id } = await params; const sp = (await searchParams) ?? {}; const client = await db.client.findUnique({ where: { id }, include: { projectTypes: { orderBy: { name: 'asc' } } } }); if (!client) notFound(); return <div className="space-y-6"><PageHeader title={`Project Types · ${client.name}`} description="Manage client-specific project types." actions={<Link href={`/clients/${client.id}`} className="btn-secondary">Back to client</Link>} />{sp.create==='1'?<ProjectTypeForm mode="create" action={createProjectTypeAction} clientId={client.id}/>:<Link href={`?create=1`} className="btn-primary inline-flex">Create project type</Link>}<div className="table-wrap"><table className="table-base"><thead className="table-head"><tr><th className="table-cell">Name</th><th className="table-cell">Code</th><th className="table-cell">Status</th><th className="table-cell">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{client.projectTypes.map(row=><tr key={row.id}><td className="table-cell">{row.name}</td><td className="table-cell">{row.code}</td><td className="table-cell"><span className={row.isActive?'badge-emerald':'badge-slate'}>{row.isActive?'Active':'Inactive'}</span></td><td className="table-cell"><div className="flex gap-2"><Link href={`/clients/${client.id}/project-types/${row.id}`} className="btn-secondary text-xs">Edit</Link><form action={toggleProjectTypeStatusAction}><input type="hidden" name="projectTypeId" value={row.id}/><button className="btn-secondary text-xs">{row.isActive?'Deactivate':'Activate'}</button></form></div></td></tr>)}{client.projectTypes.length===0?<tr><td colSpan={4} className="table-cell text-center text-sm text-slate-500">No project types found.</td></tr>:null}</tbody></table></div></div>; }
