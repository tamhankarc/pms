import Link from "next/link"; import { PageHeader } from "@/components/ui/page-header"; import { db } from "@/lib/db"; import { SubProjectForm } from "@/components/forms/sub-project-form"; import { createSubProjectAction, toggleSubProjectStatusAction } from "@/lib/actions/sub-project-actions";
export default async function SubProjectPage({ searchParams }: { searchParams?: Promise<{ projectId?: string; create?: string }>; }) { const params=(await searchParams)??{}; const selectedProjectId=params.projectId??''; const showCreate=params.create==='1'; const [projects, subProjects] = await Promise.all([db.project.findMany({ where:{isActive:true}, include:{client:true}, orderBy:[{name:'asc'}] }), db.subProject.findMany({ where:selectedProjectId?{projectId:selectedProjectId}:{}, include:{ project:{include:{client:true}}, assignments:{include:{user:true}} }, orderBy:[{project:{name:'asc'}},{name:'asc'}] })]); return <div className="space-y-6"><PageHeader title="Sub Projects" description="Create and maintain Sub Projects under a project. User assignment is now managed separately." actions={<Link className="btn-primary" href={`/sub-project?create=1${selectedProjectId ? `&projectId=${selectedProjectId}` : ''}`}>Create Sub Project</Link>} /><div className="card p-4"><form method="get" className="grid gap-3 md:grid-cols-[1fr_auto_auto]"><select className="input" name="projectId" defaultValue={selectedProjectId}><option value="">All projects</option>{projects.map(project=><option key={project.id} value={project.id}>{project.name} · {project.client.name}</option>)}</select><button className="btn-secondary" type="submit">Apply</button><Link href="/sub-project" className="btn-secondary">Reset</Link></form></div>{showCreate?<SubProjectForm mode="create" projects={projects.map(project=>({id:project.id,name:project.name,clientName:project.client.name}))} action={createSubProjectAction} initialValues={{projectId:selectedProjectId}}/>:null}<div className="table-wrap"><table className="table-base"><thead className="table-head"><tr><th className="table-cell">Sub Project</th><th className="table-cell">Project</th><th className="table-cell">Assigned People</th><th className="table-cell">Status</th><th className="table-cell">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{subProjects.map(subProject=><tr key={subProject.id}><td className="table-cell"><div className="font-medium text-slate-900">{subProject.name}</div><div className="text-xs text-slate-500">{subProject.description || '—'}</div></td><td className="table-cell">{subProject.project.name}<div className="text-xs text-slate-500">{subProject.project.client.name}</div></td><td className="table-cell">{subProject.assignments.map(row=>row.user.fullName).join(', ') || '—'}</td><td className="table-cell"><span className={subProject.isActive?'badge-emerald':'badge-slate'}>{subProject.isActive?'Active':'Inactive'}</span></td><td className="table-cell"><div className="flex gap-2"><Link className="btn-secondary text-xs" href={`/sub-project/${subProject.id}`}>Edit</Link><form action={toggleSubProjectStatusAction}><input type="hidden" name="subProjectId" value={subProject.id}/><button className="btn-secondary text-xs">{subProject.isActive?'Deactivate':'Activate'}</button></form></div></td></tr>)}{subProjects.length===0?<tr><td colSpan={5} className="table-cell text-center text-sm text-slate-500">No Sub Projects found.</td></tr>:null}</tbody></table></div></div>; }
